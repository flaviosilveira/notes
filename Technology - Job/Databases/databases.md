# Databases

```
In general, if the likelihood of needing to perform a DELETE is low, and the SELECT statement is lightweight and uses well-indexed columns, performing the SELECT first might save resources by avoiding unnecessary locking and transaction costs associated with the DELETE. 
```
